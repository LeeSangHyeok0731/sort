# 🛠️ Sorting Visualizer 개발 로그 및 문제 해결 기록

이 문서는 프로젝트 진행 중 발생한 주요 이슈와 해결 과정을 기록합니다. 매 수정 시마다 업데이트됩니다.

---

## 📅 2026. 01. 06

### 1. 🚀 애니메이션 방식 개선: 값 변경에서 물리적 이동으로

- **문제**: 단순히 막대의 높이만 바뀌는 방식은 시각적으로 단조롭고 알고리즘의 동작(Swap)을 직관적으로 보여주지 못함.
- **해결**: `framer-motion` 라이브러리를 도입하고, 각 배열 요소를 `{ id, value }` 형태의 객체로 전환. `layout` 프로퍼티를 사용하여 데이터 순서가 바뀔 때 막대가 부드럽게 옆으로 이동하도록 구현.

### 2. ⚡ 실시간 배속 조절 및 제어 기능 추가

- **문제**: 정렬 과정을 더 빠르거나 느리게 관찰할 필요가 있음. 또한 정렬 도중 멈추거나 커스텀 데이터를 넣고 싶은 요구사항 발생.
- **해결**: 0.25x ~ 4x까지의 배속 선택 기능 구현. '중단하기' 버튼을 통해 즉시 초기화 기능 추가. 숫자 입력을 통한 커스텀 배열 및 무작위 생성 개수 지정 기능 구현.

### 3. 🐛 셸 정렬(Shell Sort) 시각화 버그

- **문제**: 셸 정렬 완료 후 화면상의 막대들이 올바른 순서로 정렬되지 않은 상태로 멈춤.
- **원인**: 셸 정렬의 'Shift' 방식 로직이 시각화용 중간 단계(Step) 저장 시 객체 복사와 인덱스 참조가 꼬임.
- **해결**: 셸 정렬의 이동 로직을 'Swap' 기반으로 재작성하여 각 막대 객체의 고유 ID가 유지되도록 수정.

### 4. ⚠️ React 'Duplicate Key' 에러 (중요)

- **문제**: 정렬 도중 또는 초기화 시 `Encountered two children with the same key` 에러 발생.
- **원인 1**: 새로운 배열 생성 시 ID 카운터가 리셋되어 이전 막대들과 ID가 겹침.
- **원인 2 (심각)**: 병합/기수/계수 정렬 등 비비교 정렬은 값을 덮어씌울 때, 일시적으로 한 배열 내에 같은 객체(ID)가 두 번 존재하게 됨.
- **해결**:
  - `useRef` 기반의 전역 ID 카운터를 도입하여 세션 내 유일한 ID 보장.
  - **모든 알고리즘을 'Swap' 기반 배치 시스템으로 전환.** 값을 덮어씌우는 로직 대신, 배열 내에서 타겟 아이템을 찾아 위치를 맞바꾸도록 수정하여 항상 유일한 ID들만 화면에 존재하도록 강제함.

### 5. 🏎️ 고속 정렬 시 상태 업데이트 불일치

- **문제**: 4배속 등 고속 설정 시 상태 업데이트가 비동기적으로 겹쳐 에러 발생 확률 증가.
- **해결**: `useEffect` 내에서 이전 상태가 확실히 반영된 후 다음 단계로 넘어가는 순차적 타임아웃 로직 검토 및 ID 충돌 방지 로직 강화.

### 6. 🚀 배속 옵션 확장 (8x, 16x)

- **문제**: 데이터 양이 많아질 경우(100개 등) 4배속으로도 정렬 과정이 길게 느껴짐.
- **해결**: 8배속 및 16배속 옵션을 추가하여 대량의 데이터도 순식간에 정렬되는 과정을 관찰할 수 있도록 개선.

### 7. 📈 데이터 규모 조정 및 최적화 (최대 1,000개)

- **문제**: 10,000개는 브라우저 성능 및 시각적 명확성 측면에서 과도함.
- **해결**: 최대 생성 개수를 1,000개로 조정하여 성능과 시각적 효과 사이의 최적의 균형을 맞춤.
  - **조건부 애니메이션**: 데이터 개수가 100개를 초과할 경우 `framer-motion`의 `layout` 애니메이션을 비활성화하여 렌더링 부하를 줄임.
  - **레이아웃 최적화**: 데이터 개수에 따라 막대 사이의 간격(`gap`)을 조정.

### 8. 🖼️ 대규모 데이터 전용 확대 모드(Modal) 도입

- **문제**: 데이터가 100개를 넘어갈 경우 일반적인 레이아웃으로는 개별 막대 식별이 어렵고 시각적 몰입감이 떨어짐.
- **해결**:
  - **자동/수동 확대**: 데이터 개수를 100개 초과하여 생성할 경우 자동으로 화면 전체를 사용하는 '확대 모드(Modal)'로 전환되도록 구현.
  - **풀스크린 레이아웃**: 확대 모드에서는 화면 높이의 60% 이상을 막대 그래프에 할당하여 수천 개의 데이터도 명확하게 관찰 가능.
  - **모달 UX**: 애니메이션 조작창과 상태 정보를 넓은 화면에 최적화하여 배치하고, 언제든 축소(Esc/닫기)할 수 있는 기능 제공.

### 9. ⚙️ 알고리즘별 맞춤 파라미터 제어 기능

- **문제**: 셸 정렬의 간격(Gap)이나 버킷 정렬의 버킷 개수 등 알고리즘의 효율성에 큰 영향을 미치는 요소를 사용자가 직접 조정할 수 없음.
- **해결**:
  - **셸 정렬 간격 제어**: 사용자가 직접 원하는 간격 시퀀스(예: Ciura 시퀀스 등)를 입력하여 정렬 과정에 미치는 영향을 관찰할 수 있게 함.
  - **버킷 개수 슬라이더**: 버킷 정렬 선택 시 실시간 슬라이더를 통해 버킷 개수를 1개에서 20개까지 조절할 수 있는 기능 추가.
  - **동적 UI**: 선택된 알고리즘에 따라 필요한 설정창만 나타나도록 컨트롤 패널 최적화.

### 10. 🎲 무작위 생성 옵션 다양화 (중복 없음 vs 완전 랜덤)

- **문제**: 단순히 무작위 값을 생성하는 것 외에도, 특정 숫자의 순열(Shuffle)을 정렬하는 과정을 관찰하고 싶은 요구사항 발생.
- **해결**:
  - **중복 없음 (순열)**: 1부터 N까지의 숫자를 생성한 뒤 Fisher-Yates 셔플 알고리즘으로 무작위로 섞는 방식. 모든 막대의 높이가 달라 시각적 구분이 명확함.
  - **중복 있음 (완전 랜덤)**: 기존 방식대로 일정 범위 내에서 완전히 무작위로 값을 추출. 같은 높이의 막대가 존재할 수 있어 실제 데이터의 중복 상황을 테스트 가능.
  - **UI 연동**: 컨트롤 패널에서 두 모드를 쉽게 전환할 수 있는 선택 버튼 추가.

### 11. 🛡️ 최소 데이터 핸들링 및 유효성 검사

- **문제**: 데이터가 0개 또는 1개일 때 정렬을 시작하면 시각적 오류나 알고리즘 무한 루프 가능성 존재.
- **해결**:
  - **입력 범위 확장**: 무작위 생성 시 0개, 1개도 생성할 수 있도록 하한선 조정.
  - **버튼 활성화 로직**: 데이터가 2개 미만일 경우 '정렬 시작' 버튼을 자동으로 비활성화(Disabled) 처리하여 불필요한 연산 방지.
  - **안전성 확보**: 예외적인 사용자 입력 상황에서도 정지 상태를 유지하도록 UI 피드백 강화.

---

_마지막 업데이트: 2026-01-06 11:15_
