# 🛠️ Sorting Visualizer 개발 로그 및 문제 해결 기록

이 문서는 프로젝트 진행 중 발생한 주요 이슈와 해결 과정을 기록합니다. 매 수정 시마다 업데이트됩니다.

---

## 📅 2026. 01. 06

### 1. 🚀 애니메이션 방식 개선: 값 변경에서 물리적 이동으로

- **문제**: 단순히 막대의 높이만 바뀌는 방식은 시각적으로 단조롭고 알고리즘의 동작(Swap)을 직관적으로 보여주지 못함.
- **해결**: `framer-motion` 라이브러리를 도입하고, 각 배열 요소를 `{ id, value }` 형태의 객체로 전환. `layout` 프로퍼티를 사용하여 데이터 순서가 바뀔 때 막대가 부드럽게 옆으로 이동하도록 구현.

### 2. ⚡ 실시간 배속 조절 및 제어 기능 추가

- **문제**: 정렬 과정을 더 빠르거나 느리게 관찰할 필요가 있음. 또한 정렬 도중 멈추거나 커스텀 데이터를 넣고 싶은 요구사항 발생.
- **해결**: 0.25x ~ 4x까지의 배속 선택 기능 구현. '중단하기' 버튼을 통해 즉시 초기화 기능 추가. 숫자 입력을 통한 커스텀 배열 및 무작위 생성 개수 지정 기능 구현.

### 3. 🐛 셸 정렬(Shell Sort) 시각화 버그

- **문제**: 셸 정렬 완료 후 화면상의 막대들이 올바른 순서로 정렬되지 않은 상태로 멈춤.
- **원인**: 셸 정렬의 'Shift' 방식 로직이 시각화용 중간 단계(Step) 저장 시 객체 복사와 인덱스 참조가 꼬임.
- **해결**: 셸 정렬의 이동 로직을 'Swap' 기반으로 재작성하여 각 막대 객체의 고유 ID가 유지되도록 수정.

### 4. ⚠️ React 'Duplicate Key' 에러 (중요)

- **문제**: 정렬 도중 또는 초기화 시 `Encountered two children with the same key` 에러 발생.
- **원인 1**: 새로운 배열 생성 시 ID 카운터가 리셋되어 이전 막대들과 ID가 겹침.
- **원인 2 (심각)**: 병합/기수/계수 정렬 등 비비교 정렬은 값을 덮어씌울 때, 일시적으로 한 배열 내에 같은 객체(ID)가 두 번 존재하게 됨.
- **해결**:
  - `useRef` 기반의 전역 ID 카운터를 도입하여 세션 내 유일한 ID 보장.
  - **모든 알고리즘을 'Swap' 기반 배치 시스템으로 전환.** 값을 덮어씌우는 로직 대신, 배열 내에서 타겟 아이템을 찾아 위치를 맞바꾸도록 수정하여 항상 유일한 ID들만 화면에 존재하도록 강제함.

### 5. 🏎️ 고속 정렬 시 상태 업데이트 불일치

- **문제**: 4배속 등 고속 설정 시 상태 업데이트가 비동기적으로 겹쳐 에러 발생 확률 증가.
- **해결**: `useEffect` 내에서 이전 상태가 확실히 반영된 후 다음 단계로 넘어가는 순차적 타임아웃 로직 검토 및 ID 충돌 방지 로직 강화.

### 6. 🚀 배속 옵션 확장 (8x, 16x)

- **문제**: 데이터 양이 많아질 경우(100개 등) 4배속으로도 정렬 과정이 길게 느껴짐.
- **해결**: 8배속 및 16배속 옵션을 추가하여 대량의 데이터도 순식간에 정렬되는 과정을 관찰할 수 있도록 개선.

---

_마지막 업데이트: 2026-01-06 10:44_
